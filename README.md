# learning-flask-todo-rest

```console
sudo apt install python-virtualenv
python3 -m venv venv
. venv/bin/activate
export FLASK_DEBUG=1
export FLASK_APP=app
pip install -r requirements/dev.txt
flask run
```